apiVersion: v1
kind: Service
metadata:
  name: demo-nginx
  namespace: default
  labels:
    app: demo-nginx
    managed-by: argocd
spec:
  type: ClusterIP
  selector:
    app: demo-nginx
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  sessionAffinity: None
